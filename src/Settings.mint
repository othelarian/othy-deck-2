component Settings {
  fun render {
    <div>
      "Settings aren't available right now"<br />
      "(IDEA : tarot/poker, with/without jokers, eliminate card, local storage auto save)"
    </div>
  }
}